import React from "react";
import { Text, View } from "react-native";

const AddScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>This is Add Transaction Screen.</Text>
    </View>
  );
};

export default AddScreen;
